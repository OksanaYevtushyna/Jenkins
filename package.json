{
  "name": "@connect/cp-cypress-jenkins-gam",
  "version": "1.0.0",
  "scripts": {
    "tsc": "tsc",
    "test": "cp config.test.ts config.ts && npm run clean && eslint '**/*.ts' && jest",
    "clean": "rm -rf dist && mkdir dist",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:record": "cypress run --record --key 4a91cf71-cd1f-4ed6-b479-563dcb69eafa"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.0.5",
    "@typescript-eslint/eslint-plugin": "^3.0.2",
    "@typescript-eslint/parser": "^3.0.2",
    "google-closure-compiler": "20190528.0.0",
    "path": "^0.12.7",
    "ts-loader": "^7.0.5",
    "typescript": "^3.6.3",
    "cypress": "^7.0.0"
  },
  "peerDependencies": {
    "electron": "^12.0.2"
  }
}
